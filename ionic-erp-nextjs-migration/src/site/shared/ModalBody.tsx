import LeadForm from "@/components/contact/LeadForm";

/**
 * Compatibility export retained for the source-to-target component map.
 * New code renders the single root-level ContactDialog instead.
 */
const ModalBody = () => <LeadForm variant="modal" />;

export default ModalBody;
